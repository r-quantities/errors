language: R
sudo: false
cache: packages
r:
  - oldrel
  - release
  - devel

git:
  submodules: false

warnings_are_errors: true

r_github_packages:
  - r-lib/covr@v3.0.1

after_success:
  - Rscript -e 'covr::codecov()'
